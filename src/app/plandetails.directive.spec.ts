import { PlandetailsDirective } from './plandetails.directive';

describe('PlandetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new PlandetailsDirective();
    expect(directive).toBeTruthy();
  });
});
